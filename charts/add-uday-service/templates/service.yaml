apiVersion: v1
kind: Service
metadata:
  name: {{ include "add-uday-service.fullname" . }}
  labels:
    {{- include "add-uday-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "add-uday-service.selectorLabels" . | nindent 4 }}
